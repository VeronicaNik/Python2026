result: 20/100

### 1. Формулировка задания
Цель: создать функцию `calculate_total`, принимающую список товаров (словари с ключами "название" и "цена") и возвращающую общую сумму цен.  
Ключевые требования:
- Функция с ровно одним параметром
- Возврат суммы через `return` (не `print`)
- Обработка пустого списка (возврат 0)
- Использование только базовых конструкций: `def`, цикл `for`, сложение

### 2. Результаты проверки
**Файл:** `tasks/1/Tasks_1` (строки 1-6)  
**Тесты:**
1. **Параметры функции:** Ошибка при вызове `calculate_total(покупки)` — функция требует 2 аргумента, но получен 1.
2. **Неопределённая переменная:** `result` в цикле `for` не объявлена до использования.
3. **Логика суммирования:** Цены товаров не извлекаются из списка, вместо этого используется параметр `Цена`.

**Шаги воспроизведения:**
```python
# Попытка вызова с примером из задания
покупки = [{"название": "Хлеб", "цена": 50}]
calculate_total(покупки)  # TypeError: missing 1 required positional argument
```

### 3. Сильные стороны работы
- Правильное начальное значение `total_sum = 0` (соответствует требованию для пустого списка)
- Попытка использовать цикл для обработки товаров
- Верное указание имени функции `calculate_total`

### 4. Ошибки и исправления
**Блокирующие ошибки (функция неработоспособна):**
1. **Неверное количество параметров**  
   Код: `def calculate_total(items_list, Цена)`  
   Ожидается: 1 параметр.  
   Исправление:  
   ```python
   def calculate_total(items_list):
   ```

2. **Использование необъявленной переменной**  
   Код: `for calculate_total in result:`  
   Ошибка: `result` не определён до цикла.  
   Исправление: цикл должен перебирать `items_list`:  
   ```python
   for item in items_list:
   ```

**Значимые ошибки (нарушение логики):**
1. **Некорректное извлечение цены**  
   Код: `result = total_sum + Цена`  
   Ошибка: параметр `Цена` не связан с товарами.  
   Исправление: извлекать цену из элемента списка:  
   ```python
   total_sum += item["цена"]
   ```

2. **Переопределение имени функции в цикле**  
   Код: `for calculate_total in ...`  
   Ошибка: имя функции используется как переменная цикла.  
   Исправление: использовать нейтральное имя (например, `item`).

**Минорные ошибки (стиль):**
1. **Пробелы вокруг имени функции**  
   Код: `def calculate_total (...)`  
   Стандарт PEP8: `def calculate_total(...)`.

### 5. Оценка
- **Функциональность (50%): 10/50**  
  Функция не выполняет базов
