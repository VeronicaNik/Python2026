result: 95/100

1. **Формулировка задания**  
   Требуется реализовать три функции:
   - `rectangle_info(width, height)`: возвращает кортеж `(площадь, периметр)` прямоугольника.
   - `is_adult(age)`: возвращает `True`, если возраст ≥ 18, иначе `False`.
   - `safe_div(a, b)`: возвращает `a / b`, если `b ≠ 0`, иначе `None`.

2. **Результаты проверки**  
   - **Файл**: `Tasks_1` (строки 1–18).
   - **Тесты**:
     - `rectangle_info(5, 3)` → `(15, 16)` (корректно).
     - `is_adult(18)` → `True`, `is_adult(17)` → `False` (корректно).
     - `safe_div(10, 2)` → `5.0`, `safe_div(5, 0)` → `None` (корректно).
   - **Ошибки**: Нет ошибок в функциональности.

3. **Сильные стороны работы**  
   - **Корректность**: Все функции работают согласно условию.
   - **Читаемость**: Код понятный, переменные названы логично (кроме `Pr`, см. ниже).
   - **Архитектура**: Функции изолированы, нет глобальных зависимостей.

4. **Ошибки**  
   **Минорные**:
   - **Избыточный код** (стр. 5–6): Вызов `rectangle_info(5, 3)` и `print(result)` не требуются по заданию.  
     **Исправление**: Удалить эти строки.
   - **Неидеальные названия переменных**: `Pr` вместо `perimeter` снижает читаемость.  
     **Исправление**:
     ```python
     def rectangle_info(width, height):
         area = width * height
         perimeter = 2 * (width + height)
         return (area, perimeter)
     ```

5. **Оценка**  
   - **Функциональность**: 50/50 (все задачи решены верно).  
   - **Качество кода**: 25/30 (минус 5% за избыточный код и неидеальные названия).  
   - **Стиль и тесты**: 20/20 (тесты не требовались, стиль в целом соответствует PEP8).  
   **Итого**: 95/100.

6. **Доработки**  
   Полное решение с исправлениями:
   ```python
   def rectangle_info(width, height):
       area = width * height
       perimeter = 2 * (width + height)
       return (area, perimeter)

   def is_adult(age):
       return age >= 18  # Упрощённая запись

   def safe_div(a, b):
       if b == 0:
           return None
       return a / b
   ```

---

Анализ выполнен моделью: GPT-4o.
